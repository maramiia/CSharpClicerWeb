@model BossViewModel


@section styles {
    <link href="@Url.Content("~/css/index.css")" rel="stylesheet">
}

<div class="col">
    <div id="purchased-bosses" class="purchased-bosses">
        @foreach (var boss in Model.Bosses)
        {
            if (boss.IsBuy == false)
            {
                <img src="data:image/png;base64, @Convert.ToBase64String(boss.Image)" height="100" />
            }
        }
    </div>
</div>
<div class="mt-3 back">
    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Вернуться на главную страницу</a>
</div>

@section scripts {
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="~/js/index.js"></script>
}


